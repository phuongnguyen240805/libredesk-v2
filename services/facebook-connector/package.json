{
  "name": "libredesk-facebook-connector",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": { "start": "node supervisor.js", "check": "node --check supervisor.js && node --check server.js" }
}
